---

galaxy_info:
  role_name: acme_cloudflare_dns01
  author: ndkprd
  description: "Ansible role to generate SSL certificates using DNS-01 challenge via Cloudflare."
  license: MIT
  min_ansible_version: "2.10"
  platforms:
    - name: "Fedora"
      version: [all]
  galaxy_tags: [acme, dns01, cloudflare, dns]
dependencies: []
